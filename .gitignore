node_modules/
dist/
build/
# Built UI: generated by `npm run build:ui`, copied into dist for publishing.
src/server/static/
coverage/
.vibe/

# Layer 3 code generation output: real scaffolded projects written to disk
# and verified against the Blueprint CLI. Never committed, never analysed by
# a normal `vibe-blueprint .` run on this repo (see ingest/ignore-rules.ts).
generated/
ui/test-results/
ui/playwright-report/
*.tsbuildinfo
.DS_Store
.env

# Generated by `--export`. Regenerated from the analysis on every run, so
# committing them would put a stale measurement under review.
blueprint.html
scratch-pipe-schema-to-compiler-log.jsonl
