// Fixture: DELIBERATELY MALFORMED. Excluded from tsc and ESLint on purpose.
// The parser must recover, keep whatever it can, flag the file, and never throw.
// A valid import sits first so we can prove recovery yields partial results.
import { recoverable } from './recoverable-before-the-damage';

export function halfWritten( {
  const unclosed = {
    key: 'value'

class Dangling extends {
  method( { return
}

const ) = ][ ;;
import from from from
